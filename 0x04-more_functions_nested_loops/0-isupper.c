#include "main.h"

/**
* _isupper - checks for uppercase characters
*@c: char to be checked
*Return:0 if capital else, 1
*/

int _isupper(int c)

{


	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{

		return (0);
	}
}
